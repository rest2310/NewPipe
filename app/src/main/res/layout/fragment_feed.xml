<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?attr/windowBackground">

    <LinearLayout
        android:id="@+id/refresh_root_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="?attr/windowBackground"
        android:orientation="vertical"
        android:paddingStart="12dp"
        android:paddingTop="8dp"
        android:paddingEnd="12dp"
        android:paddingBottom="8dp">

        <LinearLayout
            android:id="@+id/feed_search_bar"
            android:layout_width="match_parent"
            android:layout_height="44dp"
            android:background="@drawable/feed_search_bar_background"
            android:clickable="true"
            android:focusable="true"
            android:gravity="center_vertical"
            android:orientation="horizontal"
            android:paddingStart="16dp"
            android:paddingEnd="16dp">

            <ImageView
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:src="@drawable/ic_search"
                app:tint="@color/placeholder_foreground"
                tools:ignore="ContentDescription" />

            <org.schabi.newpipe.views.NewPipeTextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="12dp"
                android:text="@string/feed_search_hint"
                android:textColor="@color/placeholder_foreground"
                android:textSize="16sp" />
        </LinearLayout>

        <com.google.android.material.chip.ChipGroup
            android:id="@+id/feed_filter_chips"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            app:checkedChip="@id/feed_filter_all"
            app:chipSpacingHorizontal="8dp"
            app:selectionRequired="true"
            app:singleSelection="true">

            <com.google.android.material.chip.Chip
                android:id="@+id/feed_filter_all"
                style="@style/Widget.MaterialComponents.Chip.Choice"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:checked="true"
                android:text="@string/feed_filter_all"
                android:theme="@style/Theme.MaterialComponents.DayNight.Bridge"
                app:chipBackgroundColor="?attr/contrast_background_color"
                app:chipMinTouchTargetSize="40dp" />

            <com.google.android.material.chip.Chip
                android:id="@+id/feed_filter_favorites"
                style="@style/Widget.MaterialComponents.Chip.Choice"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/feed_filter_favorites"
                android:theme="@style/Theme.MaterialComponents.DayNight.Bridge"
                app:chipBackgroundColor="?attr/contrast_background_color"
                app:chipMinTouchTargetSize="40dp" />

            <com.google.android.material.chip.Chip
                android:id="@+id/feed_filter_new"
                style="@style/Widget.MaterialComponents.Chip.Choice"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/feed_filter_new"
                android:theme="@style/Theme.MaterialComponents.DayNight.Bridge"
                app:chipBackgroundColor="?attr/contrast_background_color"
                app:chipMinTouchTargetSize="40dp" />

            <com.google.android.material.chip.Chip
                android:id="@+id/feed_filter_unfinished"
                style="@style/Widget.MaterialComponents.Chip.Choice"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/feed_filter_unfinished"
                android:theme="@style/Theme.MaterialComponents.DayNight.Bridge"
                app:chipBackgroundColor="?attr/contrast_background_color"
                app:chipMinTouchTargetSize="40dp" />
        </com.google.android.material.chip.ChipGroup>
    </LinearLayout>

    <androidx.swiperefreshlayout.widget.SwipeRefreshLayout
        android:id="@+id/swipeRefreshLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/refresh_root_view">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/items_list"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:clipToPadding="false"
            android:layoutAnimation="@anim/layout_list_item_animation"
            android:padding="@dimen/rounded_surface_spacing"
            android:scrollbars="vertical"
            android:visibility="gone"
            tools:listitem="@layout/list_stream_item"
            tools:visibility="visible" />

    </androidx.swiperefreshlayout.widget.SwipeRefreshLayout>

    <Button
        android:id="@+id/new_items_loaded_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@id/swipeRefreshLayout"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="5sp"
        android:text="@string/feed_new_items"
        android:textSize="12sp"
        android:theme="@style/ServiceColoredButton"
        android:visibility="gone"
        tools:visibility="visible" />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:gravity="center"
        android:orientation="vertical">

        <ProgressBar
            android:id="@+id/loading_progress_bar"
            style="@style/Widget.AppCompat.ProgressBar.Horizontal"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:indeterminate="true"
            android:minWidth="128dp"
            android:visibility="gone"
            tools:visibility="visible" />

        <org.schabi.newpipe.views.NewPipeTextView
            android:id="@+id/loading_progress_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:text="∞/∞"
            android:textAppearance="@style/TextAppearance.AppCompat.Title"
            android:textSize="16sp"
            android:visibility="gone"
            tools:text="1/120"
            tools:visibility="visible" />
    </LinearLayout>

    <include
        android:id="@+id/error_panel"
        layout="@layout/error_panel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:visibility="gone"
        tools:visibility="visible" />

    <include
        android:id="@+id/empty_state_view"
        layout="@layout/list_empty_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layout_marginTop="50dp"
        android:visibility="gone"
        tools:visibility="visible" />
</RelativeLayout>
